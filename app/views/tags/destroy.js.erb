<!--// モーダルの書き換え-->
$('[data-model_name="tag"]').filter('[data-id="<%= @tag.id %>"]').remove();

<!--// すでに表示されているbooksを書き換え-->
$('#booksIndex').html('<%= j(render partial: "books/book", collection: current_user.books, as: :book) %>')

